<?xml version="1.0" encoding="UTF-8"?>
<blueprint xmlns="http://www.osgi.org/xmlns/blueprint/v1.0.0"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="
                http://www.osgi.org/xmlns/blueprint/v1.0.0 http://www.osgi.org/xmlns/blueprint/v1.0.0/blueprint.xsd
                http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0 http://aries.apache.org/schemas/blueprint-cm/blueprint-cm-1.1.0.xsd
                http://camel.apache.org/schema/blueprint http://camel.apache.org/schema/blueprint/camel-blueprint.xsd">

    <property-placeholder id="my-id" persistent-id="BlueprintBugTest" update-strategy="reload" xmlns="http://aries.apache.org/blueprint/xmlns/blueprint-cm/v1.1.0" >
        <default-properties>
            <property name="helloBean.say" value="Defaut Value from property-placeholder" />
        </default-properties>
    </property-placeholder>

    <bean id="helloBean" class="com.pronoia.camel.HelloBean">
        <property name="say" value="${helloBean.say}"/>
    </bean>

    <camelContext xmlns="http://camel.apache.org/schema/blueprint">
        <route>
            <from uri="direct://trigger"/>
            <choice>
                <when>
                    <simple>${body} == "bean"</simple>
                    <log message="choice == bean" />
                    <setBody>
                        <method ref="helloBean" method="hello"/>
                    </setBody>
                </when>
                <otherwise>
                    <log message="choice == otherwise" />
                    <setBody>
                        <constant>{{helloBean.say}}</constant>
                    </setBody>
                </otherwise>
            </choice>

            <to uri="mock://result"/>
            <log message="Body: ${body}"/>
        </route>
    </camelContext>

</blueprint>
